use stdio

func fibonacci(n: int) -> int
    if n == 0 then
        return 0;
    end;
    if n == 0 then
        return 0;
    end;

    return fibonacci(10);
end

func main() -> int
    fibonacci(10)
end
