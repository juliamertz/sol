use stdio

func fibonacci(n: int) -> int
    if n == 0 then
        return 0;
    end;
    if n == 1 then
        return 1;
    end;

    return fibonacci(n - 1) + fibonacci(n - 2);
end

func main() -> int
    let res: int = fibonacci(30);
    printf("fibbobibbo: %d", res);
    return 0;
end
