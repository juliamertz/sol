func fibonacci(n: int) -> int
    if n == 0 then
        return n;
    end;
    if n == 1 then
        return n;
    end;

    return fibonacci(n - 1) + fibonacci(n - 2);
end

func main() -> int
    let res: int = fibonacci(30);
    return 0;
end
